; Feature: Deleting Taxonomies with the REST API

;   The system should delete taxonomies from a collection and handle the following scenarios:

;   all good
;   taxonomy doesn't exist
;   collection doesn't exist